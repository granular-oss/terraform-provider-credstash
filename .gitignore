# If you prefer the allow list template instead of the deny list, see community template:
# https://github.com/github/gitignore/blob/main/community/Golang/Go.AllowList.gitignore
#
# Binaries for programs and plugins
*.exe
*.exe~
*.dll
*.so
*.dylib

# Test binary, built with `go test -c`
*.test

# Output of the go coverage tool, specifically when used with LiteIDE
*.out

# Dependency directories (remove the comment below to include it)
vendor/

# Go workspace file
go.work

/terraform-provider-credstash

# Ignore vscode
.vscode

# Ignore credstash.log
credstash.log

# Ingore test terraform
integration_test/tf/.terraform
integration_test/tf/terraform.tfstate*
#Ignore lock file as we always want integration to install latest provider
integration_test/tf/.terraform.lock.hcl
integration_test/tf/providers